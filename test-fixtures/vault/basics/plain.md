# Plain Note

This is the simplest possible note. No frontmatter, no links, no tags.

It exists so we have a baseline file to read with `view` and to verify that
tools work on minimal content.

The total contents of this file are stable — tests may assert on exact line
counts or substring matches.
