# Note With Inline Tags

This note carries inline tags in body text. Obsidian's MetadataCache
extracts these into the file's `tags` array.

Tags present:

- #alpha
- #beta
- #project/april (nested tag — Obsidian indexes both `project` and
  `project/april` depending on which API you call)

Repeat of #alpha here for good measure (Obsidian deduplicates).

End of file.
