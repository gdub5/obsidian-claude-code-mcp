# Three Levels Deep

Path: `nested/deep/three/levels.md`

This file's only purpose is to verify:
1. `view` works on deeply nested paths
2. `view` of a parent directory shows subfolders, not just direct files
3. Path normalization handles nested separators correctly
